export type ProjectSectionType = {
  icon: string;
  projectName: string;
  preview: string;
  desc: string;
  websiteName?: string;
  link?: string;
};

export type PreviewPhotoType = {
  image: string;
  alt: string;
};
